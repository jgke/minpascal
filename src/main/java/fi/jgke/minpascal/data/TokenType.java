package fi.jgke.minpascal.data;

public enum TokenType {
    IDENTIFIER,
    INTEGER_LITERAL,
    REAL_LITERAL,
    STRING_LITERAL,

    PLUS, MINUS, TIMES, MOD, EQUALS, NOTEQUALS, LESSTHAN, MORETHAN, LESSTHANEQUALS, MORETHANEQUALS,

    OPENPAREN, CLOSEPAREN, OPENBRACKET, CLOSEBRACKET, ASSIGN, DOT, COMMA, SEMICOLON, COLON,

    OR, AND, NOT,

    IF, THEN, ELSE, OF, WHILE, DO, BEGIN, END,

    VAR, ARRAY, PROCEDURE, FUNCTION, PROGRAM, ASSERT,

    EOF
}
